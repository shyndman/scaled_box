name: scaled_box_example
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  scaled_box:
    path: ../
